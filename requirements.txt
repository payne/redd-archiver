# Core dependencies
psaw==0.1.0
psutil==6.1.1
zstandard==0.23.0

# PostgreSQL support (required)
# psycopg3 with binary wheels and connection pooling
psycopg[binary,pool]==3.2.3

# Jinja2 template system
jinja2==3.1.6

# Performance optimizations
# orjson: 10x faster JSON parsing for thread reconstruction
orjson==3.11.4

# Build optimizations (CSS minification only)
# rcssmin: Fast CSS minification (~10x faster than csscompressor)
rcssmin==1.2.2

# Image processing for favicon generation
Pillow>=10.0.0
